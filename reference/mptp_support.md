# Extract MCMC support values

Extract MCMC support values

## Usage

``` r
mptp_support(x)
```

## Arguments

- x:

  An object of class `"mptp_mcmc"`.

## Value

A data frame with columns `node_label` and `support`.
