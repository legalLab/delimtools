# Internal: point-estimate partition from co-occurrence matrix

Internal: point-estimate partition from co-occurrence matrix

## Usage

``` r
.bgmyc_point(probmat, tip_labels, ppcutoff, colname = "bgmyc")
```
