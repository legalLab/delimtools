% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mptp.R
\name{mptp_mcmc}
\alias{mptp_mcmc}
\title{Run mPTP species delimitation (MCMC)}
\usage{
mptp_mcmc(
  tree,
  method = c("multi", "single"),
  mcmc_steps = 1000000L,
  mcmc_sample = 1000L,
  mcmc_burnin = 100000L,
  mcmc_runs = 1L,
  mcmc_credible = 0.95,
  mcmc_startnull = FALSE,
  mcmc_startrandom = FALSE,
  mcmc_startml = FALSE,
  outgroup = NULL,
  outgroup_crop = FALSE,
  pvalue = 0.001,
  minbr = 1e-04,
  seed = as.integer(Sys.time()),
  quiet = FALSE
)
}
\arguments{
\item{tree}{A path to a Newick tree file (character) or a \code{phylo}
object from \pkg{ape}.  See \code{\link{mptp}} for details.}

\item{method}{Coalescent rate model: \code{"multi"} (default) or
\code{"single"}.}

\item{mcmc_steps}{Integer. Total MCMC steps per run (default 1,000,000).}

\item{mcmc_sample}{Integer. Sampling frequency (default 1,000).}

\item{mcmc_burnin}{Integer. Steps discarded as burn-in (default 100,000).}

\item{mcmc_runs}{Integer. Number of independent runs (default 1).}

\item{mcmc_credible}{Numeric. Credible interval (default 0.95).}

\item{mcmc_startnull}{Logical. Start from the null (one-species) model.}

\item{mcmc_startrandom}{Logical. Start from a random delimitation.}

\item{mcmc_startml}{Logical. Start from the ML delimitation.}

\item{outgroup}{Character. Outgroup taxon name for rooting (optional).}

\item{outgroup_crop}{Logical. Remove outgroup after rooting (default
\code{FALSE}).}

\item{pvalue}{Numeric. LRT significance threshold (default 0.001).}

\item{minbr}{Numeric. Minimum branch length threshold (default 0.0001).}

\item{seed}{Integer. Random seed (default: current time).}

\item{quiet}{Logical. Suppress C-level progress messages (default
\code{FALSE}).}
}
\value{
An object of classes \code{c("mptp_mcmc", "mptp_ml")}, a list with
all fields of \code{"mptp_ml"} plus:
\describe{
\item{\code{support}}{Data frame with columns \code{node_label} and
\code{support} (0–1); posterior probability of each inner node being
a coalescent root.}
\item{\code{mcmc_steps}}{Integer.}
\item{\code{mcmc_sample}}{Integer.}
\item{\code{mcmc_burnin}}{Integer.}
\item{\code{mcmc_runs}}{Integer.}
\item{\code{seed}}{Integer. Actual seed used.}
}
}
\description{
Calls the mPTP MCMC engine directly via \code{.Call()}. Returns ML species
assignments plus per-node posterior support values.
}
\examples{
\dontrun{
result <- mptp_mcmc(
  "tree.nwk",
  mcmc_steps    = 5000000L,
  mcmc_burnin   = 500000L,
  mcmc_runs     = 3L,
  seed          = 42L,
  outgroup      = "Outgroup1",
  outgroup_crop = TRUE
)
print(result)
head(result$support)
}

}
\seealso{
\code{\link{mptp}}, \code{\link{mptp_tbl}}
}
